# Supabase connection (required)
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# JWT secret — use a long random string (required)
JWT_SECRET=troque-por-uma-string-aleatoria-muito-longa-aqui

# App environment
NODE_ENV=production

# Cron job secret (optional but recommended)
CRON_SECRET=outro-segredo-aqui

# CORS — restrict to your frontend domain
ALLOWED_ORIGIN=https://verde-azul.vercel.app

# Local dev port (optional, default 3000)
PORT=3000
